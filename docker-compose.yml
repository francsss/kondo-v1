services:
  postgres:
    image: postgres:16-alpine
    container_name: kondo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: kondo
      POSTGRES_PASSWORD: kondo_password
      POSTGRES_DB: kondo
    ports:
      - "5432:5432"
    volumes:
      - kondo_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kondo -d kondo"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  kondo_postgres_data:
